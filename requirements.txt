torch>=2.0.0,<3.0.0
transformers>=4.30.0,<5.0.0
accelerate>=0.20.0,<1.0.0
einops>=0.7.0,<1.0.0
numpy>=2.0.0,<3.0.0
pybloom-live>=3.1.0,<5.0.0
plotly>=5.0.0,<6.0.0
gradio>=4.0.0,<5.0.0
# Gradio 4.x calls Jinja2Templates.TemplateResponse(template_name, context). Starlette 1.0
# changed the signature to TemplateResponse(request, name, context), which causes 500 errors
# (Jinja2 cache key becomes an unhashable dict). Keep FastAPI/Starlette on the 0.115/0.4x line.
fastapi>=0.115.0,<0.116.0
starlette>=0.40.0,<0.46.0
pandas>=2.0.0,<3.1.0
scikit-learn>=1.3.0,<2.0.0
requests>=2.28.0,<3.0.0
pyfaidx>=0.8.0,<1.0.0
# Python 3.13+: audioop was removed from stdlib; needed for gradio/pydub
audioop-lts>=0.2.0
